{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "node dist/main.js", 
    "releaseCommand": "npx prisma migrate deploy && npx prisma generate"
  }
}
